
反思报告
作者： Holiday
日期： 2026-07-08


一、Superpowers 技能：


发挥关键作用的技能

1. brainstorming — “一问一答”把模糊想法变成了清晰蓝图

这个技能是本次项目最有价值的一环。它设定的工作节奏——一次只提一个问题，每次给两三个选项，每个设计节点都要我确认——让我从一个“把原项目稍微扩一扩”的笼统想法，逐步形成了一份 完整 SPEC。

没有这个强制性流程，我大概率会跳过详细设计直接写代码，中途发现不对劲再回头改。这在上一次做 v1.2.0 的时候已经有过教训。Minecraft 伴侣面板的设计就是一个典型例子：如果不是通过 brainstorming 反复追问既然游戏里已经能对话了，为什么还需要一个 Web 聊天界面，我很可能会造出一个实际没人用的功能。

2. test-driven-development — 不是增加负担，而是提升了产出质量

TDD 在 AI 协作环境下的表现超出了我的预期。最初我觉得让 AI 写测试是额外的时间开销，但实际结果是，测试让 AI 生成代码的可靠性明显提高，减少了我人工核查的时间。

一个具体的例子是 Task 7 的 Dashboard stats。测试发现字符计数与预期不符，写的是 54 个字符，实际输出了 57 个。如果这个差异不是被测试代码捕获，而是留到用户使用时才发现，排查成本会高很多。六个 subagent 分别在各自的工作区写代码、跑测试，最终合并后没有出现任何一个测试回归失败。这体现了 TDD 在多人并行开发场景中的经典价值，而 Superpowers 的流程确保了它被严格执行。

3. using-git-worktrees — 让真正的并行开发成为可能

同时维护五个 worktree，五个 subagent 在各自隔离的目录中修改文件，这个工作流本身就很有工程意义。合并时，只有 app.py 出现了一处冲突（Task 7 的 Dashboard 端点和 Task 3 的 Diary 路由都注册在了这个文件里），其他文件全部零冲突合并。如果没有 worktree 做环境隔离，多个 agent 互相覆盖代码的风险是现实存在的。

4. writing-plans — 接口契约在多方协作中的价值

PLAN 里每个 Task 的“Interfaces: Consumes / Produces”字段是我认为设计上最被低估的部分。它让每个 subagent 在不了解其他 agent 存在的情况下，依然能够正确对接。比如 Task 3（Diary）的 POST /api/diary/submit 返回结构是 {corrections, score, highlighted_expressions}，Task 10（DiaryPage）就是严格按照这个接口来实现的，尽管它们在不同的 worktree 里，由不同的 agent 负责。

效果有限的技能

1. writing-skills

这个技能在本次项目中没有用上。它是面向插件开发者的功能，对于这个期末项目来说，它出现在技能列表里更多是出于功能完整性的考量，而不是实际需要。

2. receiving-code-review

在两阶段评审中，我发现自己更多地依赖人工审查，而不是这个技能。原因是 Review 技能在检查 AI 生成的代码时，倾向于指出风格问题而非逻辑错误。而在这个项目里，风格并不重要（前端使用了固定的 NES.css），真正关键的是“API 的行为是否符合 SPEC 的定义”。因此我最终改为手动对照 PLAN 和 SPEC 的 checklist 来进行逐项验证。

二、Subagent 驱动的工作流观察

这些智能体能独立运行多久而不偏离目标？

在分配给 subagent 的六个任务里，有五个在没有任何人工干预的情况下，大约六到八分钟内独立完成了 Task。唯一需要我介入的是 Task 7，在第一个 agent 遇到障碍之后，我重新分配了第二个 agent。

我认为 subagent 能否保持方向不偏离，决定性因素是 PLAN 的颗粒度。如果一个 Task 的描述已经细化到“写一个特定文件 + 写好对应测试 + 跑通 + 提交”，那么 agent 的工作范围是清晰且封闭的。但如果任务描述只是“实现 Diary 模块”，没有确切文件路径和接口约定，agent 就很容易在执行中自由发散。

什么样的 Task 颗粒度是合适的？

经过这个项目，我的体会是：合适的颗粒度 = 一个可以独立测试的 API 端点组加上对应的测试文件。

· 切得过细，比如每个函数都单独成一个 Task，会让 PLAN 过于庞大，agent 之间的接口约定开销反而超过实际实现成本。
· 切得过粗，比如把整个子系统作为一个 Task，agent 就可能在某个细节上卡住或方向走偏而无人及时纠正。
· 当前的颗粒度（例如 Task 2 包含了 Vocab Vault 的八个端点，以及 vault.py、ai_definer.py、router.py）既保持了功能的内聚性，也允许在 Task 结束时进行完整的回归测试。


三、SPEC 和 PLAN 质量如何影响最终实现

一个规约不清导致 subagent 走偏的真实案例

最典型的例子是冷启动验证中发现的 PLAN 缺陷 #2：vocab/add 端点中 api_key 字段的语义不一致。

PLAN 里写的测试断言是不带 api_key 返回 400 错误，但 PLAN 中 router 的实现却将 api_key 设计为可选字段。Gemini CLI 在跑这个测试时，面临一个选择：是按照测试来修改实现，还是按照实现来修改测试？

它选择了更合理的一边（修改测试，因为 api_key 本就应该是可选的，用户可能只想保存单词而无需使用 AI 释义）。但这个问题的本质，是 SPEC 在前期没有明确说明 api_key 到底是必传还是可选。这个决定是在写 PLAN 的时候做出的，但只体现在了实现代码中，没有同步回 SPEC。

这形成了一个典型的规约到计划到实现信息衰减链：我在写 SPEC 时默认 api_key 是可选字段（因为从 Minecraft 面板收藏单词的场景下不需要 Key），但在写 PLAN 的测试代码时忘了这个前提，写出了不带 api_key 应报400的断言。

由此得出的教训是：SPEC 不应该在写完 PLAN 之后就被当作一份不再修改的只读文档。PLAN 中任何对设计的细化，都应该及时回写到 SPEC 中保持同步。


四、最有效的 Prompt 和 Context 策略

策略一：在 Subagent Prompt 中明确指定工作目录

我给每个 subagent 的 prompt 都以一句“Your worktree is at: C:/Users/Holiday/Desktop/agent-task3-diary”开头。这直接消除了 agent 对“我应该操作哪个目录”的歧义，避免了不小心改动到主目录文件的风险。

策略二：在 PLAN 中给出完整的代码示例，而非文字描述

writing-plans 中“No Placeholders”的规则——禁止出现 TBD、TODO 或“implement later”这类只描述不展示代码的步骤——是 PLAN 能被 subagent 直接执行的前提。Task 2 的 PLAN 中包含了大约 250 行 vault.py 代码和约 100 行测试代码，agent 不需要自己去“设计”，只需要照着“实现”。这消除了 agent 最常见的一类错误来源：设计层面的偏差。

策略三：用先写测试作为上下文的锚点

每个 subagent 的 prompt 都以“先把会失败的测试写出来”开头。这让 agent 在动手写任何实现代码之前，必须先理解什么是正确的行为。测试代码在这里变成了用代码写就的规格说明，比任何自然语言的描述都更加精确。


五、凭据安全与软件分发的工程启示

这迫使我想清楚了哪些问题？

1. 凭据存放在哪里并不是一个简单问题

Minebot 的 keys.json 是在项目目录迁移时才被注意到的——它一直存在于原项目根目录下，我之前从未意识到这个文件的存在。如果不是课程对凭据安全提出了明确要求，我很可能把它连同历史 commit 一起推送到 GitHub 上。

2. 软件分发不仅仅是打包一下

用 PyInstaller 打包看似一行命令的事，但实际操作中需要处理的问题包括：

· 前端构建产物的路径映射（从 backend/static/ 到 EXE 内部的 sys._MEIPASS）
· EXE 模式下 Flask 的线程管理（通过 daemon 线程和 watchdog 实现自动退出）
· 浏览器自动打开的时机控制（使用 threading.Timer 延迟一秒，确保 Flask 先启动）
· 应对 Windows SmartScreen 的拦截提示（在 README 中需要说明如何放行）

这些细节在 v1.2.0 中已经积累了一定基础，但 v2.0 引入了 keyring 依赖，这又带来了新的打包注意事项，比如 keyring 相关的 DLL 依赖处理。

3. 仅依赖 localStorage 进行数据持久化是不够的

v1.2.0 将 API Key 存放在 localStorage 中，功能上可以运行，但从安全角度看，它是明文存储的，可以通过浏览器开发者工具直接查看，且不同浏览器之间无法共享。切换到 Windows Credential Manager 不仅提升了安全性，在数据持久性和跨进程访问方面也提供了更可靠的基础。

六、如果重新来过，我会调整的地方

1. 在 brainstorming 阶段就引入数据流图。 当前的 SPEC 中有组件关系图，但缺少显式的数据流图。这导致 Vocab Vault 与其他模块的集成方式，直到实现阶段才被完全厘清。
2. 在 PLAN 阶段就进行一轮测试断言和实现逻辑的互查。 writing-plans 的自查环节并没有系统性地核对“测试断言”和“实现逻辑”的一致性。如果我在写 PLAN 时做了一次逐字段的对比，冷启动验证中发现的三个 bug 是有机会归零的。
3. Dashboard stats 端点应放在独立 router 中。 它被直接写在了 app.py 里，导致了合并阶段的文件冲突。任何新增路由，即使只有一个端点，也应该遵循独立 router 加 register 函数的模式。
4. 尽早进行一次端到端的可运行性验证。 目前后端的 pytest（51 个测试）已经全部通过，但前端和后端还没有真正连在一起跑过，npm run build 配合 Flask serve 的完整“双击 EXE”体验还未得到验证。这是接下来优先级最高的事项。

七、对 Superpowers 方法论的几点思考

Superpowers 隐含了什么样的前提？

它假设的开发路径是清晰的需求到严格的流程到可靠的产出这样一条线性推进的链条。这在大多数情况下是成立的，但在一些情况下不成立，有些需求是在实现过程中才逐渐浮现和明确的，也就是由实现过程中产生的新认知来修正需求。

Minecraft 伴侣面板的经历就属于这种情况。在此之前我并不确定PCL 内聊天 + Web 面板回顾是更优的体验组合，直到 brainstorming 阶段被反复追问才想清楚。这个发现发生在设计阶段还算及时。但如果类似的认知出现在实现阶段，Superpowers 的流程并没有提供合理的回馈通道，它在 brainstorming 之后的所有环节，都不鼓励对 SPEC 进行偏离。

这在保护工程纪律的同时，也关闭了实现中的新发现反馈给设计的可能性。我个人认为，可以在 PLAN 中增设一个设计回顾的检查点，例如在 Phase 2 完成之后，评估一下是否有在实现中获得的新认识需要反映到 SPEC 中。

这个假设在我的项目里成立吗？

在这个项目里，大部分是成立的。我的工作属于在现有项目基础上做扩展，核心需求相对明确（四个功能模块加一个串联模块加一个首页），因此 Superpowers 的线性流程是合适的。

但对于一些更具探索性质的项目，比如从零开始做一个新产品，或者研究性质的功能开发，这种线性假设可能就会显得过于刚性。在那种情况下，我会考虑在 brainstorming 之后加入一个专门的原型验证阶段，暂时不受 TDD 的约束，纯粹用来快速确认想法的可行性，然后再正式进入 SPEC → PLAN → TDD 的流程。

总结下来，Superpowers 提供了一套让我少犯错误的可靠方法，但判断什么才是对的这件事，始终是我自己的责任。这大概就是课程一直在传递的核心意思。