# AI English Learning Studio — 项目文档

> **版本:** v2.0.0 | **类型:** B · 应用类项目（AI4SE 期末项目）
> **开发方法:** Superpowers — brainstorming → SPEC → PLAN → subagent-driven TDD
> **日期:** 2026-07-08

---

## 一、项目目标

### 1.1 要解决的问题

英语学习者面临一个典型的碎片化困境：阅读工具、写作工具、口语练习工具、单词记忆工具散布在不同平台，学习数据互不相通。比如你在翻译阅读时查过一个生词，三天后在辩论练习中遇到同一个词却完全不记得——因为它从未被系统地记录下来。

### 1.2 产品定位

**AI English Learning Studio** 是一款面向中国大学生（CET-4/6 备考）的桌面英语学习应用。它在一个统一的原生桌面窗口中提供完整的英语学习闭环：

```
输入（翻译阅读） → 输出（辩论口语 + 日记写作） → 沉浸（Minecraft 交互） → 沉淀（词汇宝库）
```

### 1.3 目标用户

- **主要用户:** 备考 CET-4/6 的中国大学生
- **使用场景:** 自习时打开桌面 EXE，按需切换翻译、写作、口语、背词四大模块
- **技术门槛:** 零——下载 EXE 双击即可，首次配置 API Key 后自动保存

---

## 二、现阶段完成工作

### 2.1 功能模块

| 模块 | 状态 | 说明 |
|------|------|------|
| 🏠 **Dashboard** | ✅ 完成 | 今日学习统计（翻译字数/日记篇数/辩论场次/新增词汇）+ 连续打卡 streak + 六模块快捷入口 |
| 📖 **Novel Translator** | ✅ 完成 | 拖拽上传 TXT → DeepSeek 翻译 → CET-4/6 词汇蓝色高亮 + ⭐ 收藏到词汇宝库 + 翻译历史回溯 |
| ✍️ **English Diary** | ✅ 完成 | AI 每日话题 + textarea 写作 → DeepSeek 逐句批改（语法/用词/流畅度 1-10 分）+ 高分表达收藏 + 打卡日历 |
| ⚔️ **Debater** | ✅ 完成 | COZE Bot 英语辩论/讨论 + 浏览器语音输入/COZE TTS 输出 + 📊 AI 辩论评分 + 对话历史持久化 |
| ⛏ **Minecraft Companion** | ✅ 完成 | PCL 游戏内对话 + Web 面板只读日志 + 5s 自动轮询 + ⭐ 词汇收藏 |
| 📚 **Vocab Vault** | ✅ 完成 | 四模块词汇汇聚 + SM-2 间隔复习闪卡（翻转动画）+ 四选一测验 + CSV 导出 |
| 🔒 **凭据安全** | ✅ 完成 | Windows Credential Manager / Fernet 加密本地文件双层存储，前端不落盘明文 |
| 📦 **桌面打包** | ✅ 完成 | pywebview (Edge WebView2) 原生窗口，PyInstaller 单文件 EXE (57MB) |

### 2.2 技术指标

| 指标 | 数值 |
|------|------|
| **后端子系统** | 7 个（config + novel + diary + debater + minecraft + vocab + dashboard） |
| **前端页面** | 6 个（NES.css 紫绿黑像素复古主题） |
| **API 端点** | 30+ 个 RESTful 端点 |
| **自动化测试** | 51 个 pytest 测试，全部通过 |
| **代码提交** | 43 个 commit（含完整分支合并历史） |
| **开发周期** | 2026-07-07 → 2026-07-10（Superpowers 全流程） |
| **可复用组件** | VocabStar（⭐ 词汇收藏按钮，4 个模块共用） |

### 2.3 Superpowers 工作流执行记录

| 阶段 | 技能 | 完成情况 |
|------|------|---------|
| 1. Brainstorming | `brainstorming` | 7 轮关键迭代，4 次采纳/推翻决策 |
| 2. Spec | — | 10 章节完整 SPEC.md，含威胁模型、验收标准 |
| 3. Plan | `writing-plans` | 18-task 实现计划，含完整接口契约、TDD 步骤 |
| 4. 冷启动验证 | Gemini CLI | 12/12 tests pass，发现 3 个 PLAN 缺陷 → 已修复 |
| 5. 并行实现 | `subagent-driven-development` | 6 个 subagent，5 个 worktree 并行 |
| 6. TDD | `test-driven-development` | 严格红-绿-重构，51 tests |
| 7. 代码评审 | `requesting-code-review` | 两阶段评审（spec 合规 + 代码质量） |

---

## 三、项目设计思路

### 3.1 架构哲学：模块隔离 + 数据汇聚

每个子系统拥有完全独立的：
- **后端路由** (`systems/<name>/router.py`)
- **会话存储** (`systems/<name>/session_storage.py` → `data/<name>/`)
- **前端模块** (`systems/<name>/` — Page.jsx + store.js + api.js)
- **测试文件** (`tests/test_<name>.py`)

各模块之间通过 **Vocab Vault 词汇宝库** 进行数据汇聚——四个核心模块的 ⭐ 按钮都是词汇的采集入口，宝库是唯一的消费端（闪卡复习、测验、导出）。

### 3.2 技术选型与理由

| 层级 | 技术 | 理由 |
|------|------|------|
| **前端** | React 18 + Vite 5 + NES.css + Zustand 4 | 已有实现；NES.css 像素风格有辨识度 |
| **后端** | Python Flask 3.x | 已有实现；Python 对 LLM SDK 支持最好 |
| **AI 引擎** | DeepSeek API (翻译/批改/评分/释义) + COZE Bot API v3 (辩论/Minecraft) | 中文双语能力强，价格低 |
| **桌面壳** | pywebview (Edge WebView2) | React 零改动，原生窗口，PyInstaller 兼容 |
| **凭据存储** | keyring + Fernet 加密文件双层 fallback | 安全性 + EXE 沙箱环境兼容 |
| **打包** | PyInstaller | 单文件 EXE 分发 |
| **测试** | pytest | 51 tests，TDD 强制 |
| **CI/CD** | GitHub Actions + GitLab CI | 课程要求 |

### 3.3 Minecraft 伴侣面板设计（最具辨识度的设计决策）

传统做法是在网页里做一个聊天 UI → 替代游戏内对话。但我们选择了**相反的方向**：

- **不在 Web 里做聊天替代**——用户在 PCL 游戏内用聊天框打字，体验最自然
- **Web 面板做「监控 + 回顾 + 词汇收集」**——游戏里做不到的事（回顾历史、收藏词汇）
- **只读对话日志 + 5s 轮询**——Bot 状态、最新消息自动刷新
- **⭐ 按钮是核心价值**——PCL 聊天框里没法收藏单词，Web 面板补上这个缺口

### 3.4 凭据安全设计

双层 fallback 策略：
1. **首选:** `keyring` → Windows Credential Manager（加密存储）
2. **Fallback:** Fernet 对称加密 → `~/.ai_english_studio/keys.enc`

EXE 文件是只读的代码包。所有运行时数据写在外部目录。用户 A 用 A 的 key，用户 B 用 B 的 key，数据完全隔离。分发 EXE 不泄露任何个人数据。

---

## 四、产品需求文档（PRD）

### 4.1 用户故事（7 个，遵循 INVEST 原则）

| # | 角色 | 需求 | 价值 | 验收标准 |
|---|------|------|------|---------|
| US1 | 备考 CET-4 的学生 | 上传中文小说并获得带四六级词汇高亮的英文翻译 | 兴趣阅读中自然积累词汇 | TXT 上传后 30s 内返回翻译，词汇正确高亮 |
| US2 | 想练口语的学生 | 与 AI 就某个话题进行英语辩论 | 对抗情境中提升口语流利度 | 多轮对话无丢失，支持语音输入和音频播放 |
| US3 | 喜欢游戏的学生 | 在 Minecraft 中用英语与 AI 助手对话 | 沉浸式场景中练习英语 | 消息 2s 内回复，会话历史可回溯 |
| US4 | 想练写作的学生 | 每天写一篇英语日记并得到 AI 逐句批改 | 每日练习中提升写作能力 | 提交后 10s 内返回逐句批改 + 评分 |
| US5 | 想系统背词的学生 | 将四个模块中遇到的生词一键加入词汇宝库 | 统一管理和复习所有场景的词汇 | 点击 ⭐ 后 0.5s 内保存，闪卡复习可用 |
| US6 | 所有用户 | 首次运行时安全录入 API Key，不被泄露 | 放心使用付费 API | Key 存入加密存储，控制台/日志/文件中不可见 |
| US7 | 非技术用户 | 下载单个 EXE 文件双击即可运行 | 零配置开始学习 | 双击 → 桌面窗口打开 → 配置 Key → 开始使用 |

### 4.2 功能模块规格

#### 🏠 Dashboard（首页仪表盘）
- **输入:** 无（纯展示）
- **行为:** 显示今日学习摘要 + 7 天打卡热力图 + 模块快捷入口
- **数据来源:** 各模块 data/ 目录 JSON 文件聚合
- **错误处理:** 统计数据读取失败不影响导航

#### 📖 Novel Translator（翻译）
- **输入:** 中文 TXT 文件 + CET-4/6 词汇 TXT 文件
- **行为:** DeepSeek 翻译 → 词汇 `[[[word]]]` 标记 → 蓝色高亮渲染
- **增强:** hover 高亮词弹释义 + ⭐ 一键收藏 + 翻译历史回溯
- **错误处理:** API 鉴权失败 → 401 提示；网络超时 → 502 提示

#### ✍️ English Diary（日记）
- **输入:** 英语短文（可选：AI 每日话题）
- **行为:** DeepSeek 批改 → 返回 JSON 格式逐句纠正 + 评分 + 高分表达
- **打卡:** 每日一篇，同天多次覆盖，连续天数计算
- **错误处理:** JSON 格式不合法 → 降级显示原文

#### ⚔️ Debater（辩论）
- **输入:** 文字或浏览器语音
- **行为:** COZE Bot 异步轮询（1.5s/次，60s 超时）→ 多轮对话
- **评分:** DeepSeek 分析完整对话 → 语法/词汇/逻辑/流畅度 四项 1-10 分 + 建议
- **错误处理:** COZE 轮询超时 → 提示重试

#### ⛏ Minecraft Companion（伴侣面板）
- **输入:** Minebot 写入的 session JSON（PCL 内完成对话）
- **行为:** Web 面板只读展示 + 5s 自动刷新 + ⭐ 词汇收藏
- **约束:** 不提供发送消息 UI，消息输入在游戏内完成
- **错误处理:** 轮询失败静默重试；Bot 离线显示 "Offline"

#### 📚 Vocab Vault（词汇宝库）
- **输入:** 四模块 ⭐ 按钮 + DeepSeek AI 释义
- **行为:** 闪卡翻转复习（SM-2 间隔算法）+ 四选一测验 + CSV 导出
- **AI 释义:** 单词 + 上下文 → DeepSeek → 音标/英文释义/中文释义/例句
- **SM-2 算法:** 质量评分 0-5 → 间隔 × EF，EF 最底 1.3
- **边界:** 空宝库引导文案；重复收藏幂等；最大 2000 词

### 4.3 非功能性需求

| 类别 | 指标 | 目标 |
|------|------|------|
| **性能** | EXE 启动 | < 3s |
| **性能** | 翻译响应（2000 字） | < 30s |
| **性能** | 辩论消息延迟 | < 5s |
| **性能** | 日记批改响应 | < 10s |
| **性能** | 词汇收藏响应 | < 0.5s |
| **安全** | 凭据存储 | 加密文件 + Windows Credential Manager |
| **安全** | 凭据泄露 | 零硬编码、零 Git 提交、零日志、零终端 |
| **安全** | 数据隔离 | 不同用户数据完全独立 |
| **可用性** | 主题一致性 | NES.css 紫绿黑像素主题，全 App 统一 |
| **可用性** | 响应式 | ≥900px 双栏，<900px 单栏堆叠 |
| **分发** | 平台 | Windows 10/11 x86-64 EXE |
| **分发** | 大小 | 57MB 单文件 |

### 4.4 验收标准

| # | 功能 | 验收标准 | 状态 |
|---|------|---------|------|
| AC1 | Dashboard | 首页正确显示四模块今日统计 + streak + 快捷入口 | ✅ |
| AC2 | Diary 批改 | 提交短文 → 10s 内返回逐句批改 + 评分 | ✅ |
| AC3 | Diary 打卡 | 连续提交 3 天 → Dashboard 显示 3 天 streak | ✅ |
| AC4 | Minecraft UI | Placeholder → 伴侣面板（只读日志 + 在线状态 + 词汇收藏）| ✅ |
| AC5 | 词汇收藏 | 任意模块 ⭐ → vault.json 新增条目 | ✅ |
| AC6 | 闪卡复习 | 翻转动画正常，SM-2 间隔正确计算 | ✅ |
| AC7 | 词汇测验 | 四选一 10 题 → 显示正确率 | ✅ |
| AC8 | 凭据安全 | 应用外不含明文 key | ✅ |
| AC9 | EXE 分发 | 双击 → 桌面窗口 → Dashboard 正常显示 | ✅ |
| AC10 | 一键测试 | `make test` 51 tests pass | ✅ |
| AC11 | CI 通过 | GitHub Actions / GitLab CI unit-test pass | ⏳ |
| AC12 | 零凭据泄露 | `git grep` 未发现真实凭据 | ✅ |

### 4.5 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                  Desktop EXE (PyInstaller)                │
│                                                           │
│  ┌──────────────────────────────────────────────────┐    │
│  │        pywebview (Edge WebView2)                   │    │
│  │  ┌────────────────────────────────────────────┐   │    │
│  │  │         React 18 SPA (Vite, NES.css)        │   │    │
│  │  │                                              │   │    │
│  │  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌───┐ │   │    │
│  │  │  │ Dash │ │Novel │ │Diary │ │Debatr│ │MC │ │   │    │
│  │  │  └──────┘ └──────┘ └──────┘ └──────┘ └───┘ │   │    │
│  │  │       │        │        │        │         │   │    │
│  │  │       └────────┴─────┬──┴────────┴─────────┘   │    │
│  │  │                      │                          │   │    │
│  │  │              ┌───────┴──────┐                   │   │    │
│  │  │              │ Vocab Vault  │ (跨模块汇聚)      │   │    │
│  │  │              └──────────────┘                   │   │    │
│  │  └──────────────────────┬───────────────────────┘   │    │
│  └─────────────────────────┼───────────────────────────┘    │
│                            │ /api/*                           │
│  ┌─────────────────────────┼───────────────────────────┐    │
│  │                    Flask Backend                      │    │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌─────────────┐  │    │
│  │  │ Novel  │ │ Diary  │ │Debater │ │Mincraft Cmpn│  │    │
│  │  │ Router │ │ Router │ │ Router │ │   Router     │  │    │
│  │  └───┬────┘ └───┬────┘ └───┬────┘ └──────┬──────┘  │    │
│  │      │          │          │             │          │    │
│  │  ┌───┴──────────┴──────────┴─────────────┴──────┐   │    │
│  │  │          Vocab Vault Router                   │   │    │
│  │  └──────────────────────┬───────────────────────┘   │    │
│  │                         │                            │    │
│  │  ┌──────────────────────┴───────────────────────┐   │    │
│  │  │              Core Layer                       │   │    │
│  │  │  ┌──────────┐ ┌──────────┐ ┌──────────────┐  │   │    │
│  │  │  │coze_clnt │ │DeepSeek  │ │ keyring_store │  │   │    │
│  │  │  │(COZE v3) │ │(OpenAI)  │ │  (Fernet enc) │  │   │    │
│  │  │  └──────────┘ └──────────┘ └──────────────┘  │   │    │
│  │  └───────────────────────────────────────────────┘   │    │
│  │                                                       │    │
│  │  数据: data/novel/  data/diary/  data/debater/        │    │
│  │        data/minecraft/  data/vocab/vault.json          │    │
│  │  凭据: ~/.ai_english_studio/keys.enc                   │    │
│  └───────────────────────────────────────────────────────┘    │
│                                                               │
│  ┌───────────────────────────────────────────────────────┐   │
│  │    Minebot (可选 Node.js 组件)                          │   │
│  │    Mineflayer Bot → Minecraft Java Server              │   │
│  │    (独立进程，Web 面板读取其 session 日志)              │   │
│  └───────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 4.6 分发方式

| 形态 | 说明 |
|------|------|
| **Windows EXE** | PyInstaller 单文件，pywebview 原生窗口，57MB |
| **源码** | `git clone` + `pip install` + `npm install` + `make dev` |
| **开发模式** | Flask :5000 + Vite :5173，浏览器访问 |

### 4.7 关键依赖

```
Python: flask, flask-cors, openai, python-dotenv, keyring, 
        cryptography, pywebview, pyinstaller, pywin32
Node:   react, react-dom, react-router-dom, zustand, vite
外部:   DeepSeek API, COZE Bot API v3
```
